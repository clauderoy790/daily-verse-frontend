import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.roy.dailyverse',
  appName: 'Daily Verse',
  webDir: 'www',
  bundledWebRuntime: false,
};

export default config;
